ranges = []
with open(0) as f:
    for ran in f.read().split(","):
        ranges.append(tuple(int(r) for r in ran.split("-")))

sum_of_invalid_ids = 0
for r in ranges:
    for i in range(r[0], r[1] + 1):
        i_str = str(i)
        for n in range(1, len(i_str)//2 + 1):
            if len(set(i_str[j:j+n] for j in range(0, len(i_str), n))) == 1:
                sum_of_invalid_ids += i
                print(i)
                break
print(sum_of_invalid_ids)